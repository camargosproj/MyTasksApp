# MyTasks APP

# MyTasks is a simple task manager app.

## To run the app on localhost:
- To run the server:
```bash
cd server
npm install
npm run server
```
 - To run the client:
```bash
cd client
npm install
npm start 
```